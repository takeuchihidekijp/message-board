<div class="panel panel-default">
  <div class="panel-heading">
    メッセージ一覧
  </div>
    <div class="panel-body">
        <% @messages.each do |message| %>
        <span class="label label-info"><%= message.name %></span>
        <%= message.body %>
        <%= message.age %>
        (<%= link_to "edit", edit_message_path(message) %>
        <%= link_to "destroy", message ,method: :delete, data:{confirm:'削除してもよろしいですか?'} %>
        )
        <br>
        <% end %>
    </div>
</div>

<%= form_for(@message) do |f| %>
  <% if @message.errors.present? %>
    <div class="alert alert-danger">
      <%= alert %>
      <ul>
        <% @message.errors.full_messages.each do |full_message| %>
          <li>
            <%= full_message %>
          </li>
        <% end %> 
      </ul>
    </div>
  <% end %>
  <form class="form-horizontal">
  <div class="form-group">
   <label for="name1" class="col-sm-2 control-label">名前</label>
   <div class="col-sm-10">
   <%= f.text_field :name, :class => "form-control", :size => "1x1" %>
   </div>
   </div>
   <br>
   <div class="form-group">
   <label for="input_about1" class="col-sm-2 control-label">内容</label>
   <div class="col-sm-10">
   <%= f.text_area :body, :class => "form-control", :size => "5x10"  %>
   </div>
   </div>
   <br>
   <div class="form-group">
   <label for="input_age1" class="col-sm-2 control-label">年齢</label>
   <div class="col-sm-10">
   <%= f.text_field :age, :class => "form-control", :size => "1x1"  %>
   </div>
   </div>
   <br>
   <br>
  <div class="form-group">
  <%= f.submit class: "btn btn-primary btn-lg btn-block" %>
  </div>
  </form>
 <% end %>

   

